{
  "name": "parcel-demo",
  "version": "1.0.0",
  "description": "",
  "source": [
    "src/index.html",
    "src/disclaimer.html"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "del public/* && del .parcel-cache",
    "dev": "parcel --dist-dir public",
    "build": "npm run clean && parcel build --dist-dir public --public-url ./ --no-source-maps"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "bulma": "^0.9.4",
    "del-cli": "^4.0.1",
    "parcel": "^2.5.0",
    "posthtml-include": "^1.7.4"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.1.1",
    "bulma": "^0.9.4",
    "cash-dom": "^8.1.1",
    "js-cookie": "^3.0.1"
  }
}
